apply plugin: 'groovy'
apply plugin: 'maven-publish'

group = 'com.github.jlmelville.plugins'
version = '1.0'

dependencies {
	compile localGroovy()
	compile gradleApi()
}

publishing {
    publications {
        maven(MavenPublication) {
            from components.java
        }
    }
}
